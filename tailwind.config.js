/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{js,jsx,ts,tsx}"], // Looks for class names in all HTML files in the root directory
  theme: {
    extend: {}, // Customize or extend the default theme
  },
  plugins: [], // Add Tailwind plugins here if needed
};
